database: schemahero
name: projects
schema:
  mysql:
    primaryKey: [id]
    collation: utf16_german2_ci
    columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: data
        type: varchar (255)
        charset: latin1
        constraints:
          notNull: true